apiVersion: v1
kind: Service
metadata:
  name: inference-service
spec:
  type: NodePort
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 80          # service port
      targetPort: 8000  # container port
      nodePort: 30080   # option fixed port (otherwise auto-assigned)